// Interface for the request payload
export interface TelegramIdRequest {
    telegram_id: number; // Telegram ID as a numeric value
  }
  
  // Interface for the response payload
  export interface TokenResponse {
    token: string; // JWT token as a string
  }
  